/**
 * When deployed to Base44, we use the real Base44 SDK.
 * The SDK is automatically configured with the current App ID by the Base44 Vite plugin.
 */
import { base44 } from '@base44/sdk';

export { base44 };

